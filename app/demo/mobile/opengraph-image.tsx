/**
 * See `app/demo/opengraph-image.tsx`. A card in the parent segment does not
 * reach this one either. `/demo` having an image says nothing about
 * `/demo/mobile`, so this route names the generator itself.
 */
export { default, alt, size, contentType } from "../../opengraph-image"
